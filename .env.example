# Application Configuration
NODE_ENV=production

# Port Configuration
# The port your application will be accessible on
APP_PORT=3000

# Docker Network Configuration
# Name of the Docker network for container communication
# Use "media-stack" to connect with other media services (Lidarr, Sonarr, etc.)
# Use "bridge" to use Docker's default bridge network
# Or specify any existing Docker network name
DOCKER_NETWORK=media-stack

# Data Paths (if needed for future persistence)
# Uncomment if you need to persist data
# DATA_PATH=./data

# Next.js Configuration (optional overrides)
# HOSTNAME=0.0.0.0
